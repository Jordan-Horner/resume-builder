# Phoenix Wright demonstration workspace

This is public fictional sample data, not a real person's private workspace.
It follows the same directory contract created by `resume-builder init` so the
complete engine workflow can be evaluated without personal career information.

Generated files belong under `build/` and are intentionally ignored.
