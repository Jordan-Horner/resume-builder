[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-builder"
version = "0.12.0"
description = "Git-first career vault tooling"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12,<5",
  "httpx>=0.27,<1",
  "playwright==1.62.0",
  "pydantic>=2.8,<3",
  "pypdf>=5,<7",
  "python-jobspy==1.1.82",
  "python-docx>=1.1,<2",
  "PyYAML>=6,<7",
  "tzdata>=2025.2",
]

[project.optional-dependencies]
gmail = [
  "google-api-python-client>=2.180,<3",
  "google-auth-httplib2>=0.2,<1",
  "google-auth-oauthlib>=1.2,<2",
]
agent = [
  "pydantic-ai-slim[openrouter]>=2.37,<3",
]
telegram = [
  "pydantic-ai-slim[openrouter]>=2.37,<3",
  "python-telegram-bot>=22,<23",
  "qrcode>=8,<9",
]
web = [
  "fastapi>=0.115,<1",
  "python-multipart>=0.0.20,<1",
  "supervisor>=4.2,<5",
  "uvicorn>=0.34,<1",
]
dev = [
  "fastapi>=0.115,<1",
  "python-multipart>=0.0.20,<1",
  "build>=1.2,<2",
  "mypy>=1.15,<2",
  "pytest>=8,<10",
  "pytest-asyncio>=0.24,<2",
  "pytest-cov>=6,<8",
  "reportlab>=4,<5",
  "ruff>=0.12,<1",
  "types-PyYAML>=6,<7",
]

[project.scripts]
resume-builder = "resume_builder.cli:main"
resume-builder-web = "resume_builder.web:main"
job-puller = "job_puller.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
job_puller = ["data/*.json", "data/*.txt"]
resume_builder = [
  "resources/templates/*.html",
  "resources/templates/renderers/*.html",
  "resources/templates/resume-templates/*.yaml",
  "resources/templates/themes/*.yaml",
  "resources/templates/themes/*.html",
  "resources/templates/themes/*.css",
  "resources/workspace/*/*.md",
]

[tool.pytest.ini_options]
addopts = "-q --strict-markers --cov=resume_builder --cov-report=term-missing --cov-fail-under=75"
testpaths = ["tests"]
markers = [
  "browser: requires the installed Playwright Chromium runtime",
]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B", "UP", "RUF"]

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
disallow_untyped_defs = true
ignore_missing_imports = true
warn_redundant_casts = true
warn_unused_ignores = true
